# Claude Code Instructions

## Git
- After making code changes, automatically commit and push without waiting to be asked.
- Stage only the files that were changed (never `git add -A` or `git add .`).
- Write concise, descriptive commit messages that explain the *why* of the change.
